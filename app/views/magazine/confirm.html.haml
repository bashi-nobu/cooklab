.a-create
  - if @crud_patarn == 'create'
    .a-create__title
      メルマガ有料会員登録
    .a-create__route
      %p.now
        お届け先住所登録
      %p
        >>
      %p
        クレジットカード情報登録
      %p
        >>
      %p
        登録完了
  - elsif @crud_patarn == 'update'
    .a-create__title
      お届け先の変更
  .a-create__form
    = form_for @magazine_address, url: set_crud_path(@crud_patarn), method: set_crud_method(@crud_patarn) do |f|
      .a-create__form__text
        下記の内容でお間違えないですか？
        %br
        .a-create__form__magazine-box.confirm-box
          .field
            %u
              宛先名
            %p.input_confirm
              = @magazine_address.address
            = f.hidden_field :address
          .field
            %u
              郵便番号
            %p.input_confirm
              = "〒#{@magazine_address.zipcode.to_s[0..2]}-#{@magazine_address.zipcode.to_s[3..6]}"
            = f.hidden_field :zipcode
          .field
            %u
              都道府県
            %p.input_confirm
              = @magazine_address.pref
            = f.hidden_field :pref
          .field
            %u
              市区町村番地
            %p.input_confirm
              = @magazine_address.city_address
            = f.hidden_field :city_address
          .field
            %u
              マンション名/建物名
            %p.input_confirm
              = @magazine_address.building
            = f.hidden_field :building
          
      .a-create__form__actions
        = button_tag "登録",class: "submit__btn magazine-form-btn"
